<%= form_for([@project, @project.comments.build] ) do |f| %>

    <%= render 'shared/error_messages', object: f.object %>
    <div class="field">
        <%= f.label :body, "コメント" %><br />
        <%= f.text_area :body, cols: 20, rows: 3 %>
        
    </div>
        <%= f.submit "Post", class: "btn btn-primary" %>
<% end %>