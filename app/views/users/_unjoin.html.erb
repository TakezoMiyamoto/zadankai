<%= form_for(current_user.joining_relationships.find_by(joined_project_id: @project.id),
             html: { method: :delete },
             remote: true) do |f| %>
  <%= f.submit "Unjoin", class: "btn" %>
<% end %>